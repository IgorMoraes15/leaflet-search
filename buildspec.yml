version: 0.2

phases:
  install:
        runtime-versions:
          nodejs: 10
  pre_build:
    commands:
      - echo Bora fazer o build ...
  build:
    commands:
      - echo Build started on `date`
      - npm install
  post_build:
    commands:
      - echo acabou `date`